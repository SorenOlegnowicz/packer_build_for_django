description "default_django"

start on (filesystem)
stop on runlevel [016]

respawn
console log
setuid ubuntu
setgid ubuntu

# change into the directory which contains the manage.py
chdir /home/ubuntu/project/

env DEBUG=false

# first connect to gunicorn in the virtualenv
# then connect to the django application
exec /home/ubuntu/virtualized/bin/gunicorn [app_name].wsgi:application -b 127.0.0.1:8002 -w 4
